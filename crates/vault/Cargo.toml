[package]
version = "0.10.2"
name = "vault"
edition = "2021"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
trash = "5"

[dev-dependencies]
tempfile = "3"
