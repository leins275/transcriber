# Test wave notes — 260910-meeting-type-in-filename (for the evaluator)

Coverage gaps to adjudicate:
- **T7**: `api.ts::updateVaultEntry` sending `kind: update.kind ?? null` and `types.ts::MeetingUpdate.kind?` have NO failing test anywhere. T7's Done-when names `src/api.test.ts`, but that file has no `updateVaultEntry` coverage at all today, so "green" there is vacuous. T5 (Rust harness) and T8 (editor payload) do not exercise the TS `?? null` line. Consider assigning an `api.test.ts` case in the gate.
- **T6**: the TS mirror collapses `EmptyType` and `TooManySeparators` to `null`; only Rust distinguishes them. Intended (display-only), but the UI can never explain *why* a dropped file went unsorted from this parser alone.

Notes carried from the authors:
- T1: the compiler names the enum `MeetingNameRejection` in diagnostics (a re-export alias in `manage.rs`); same enum. `Rejection::all().len()` 9 → 13 is a structural count, kept per the file's existing convention. `Display` strings asserted by substring, not exact prose.
- T6: `run type` stays red until `ParsedFileName` gains `kind`; the vitest red is assertion-level, not compile-level.
- T7: `260812 - Title -` today parses as title `Title -`; the new grammar must reject it. Folder-name section counts are one less than file-name counts.
- T6/T7 each keep 1–3 multi-input cases (hand-parameterized), matching the files' existing style.

Immediately-green cases: only pre-existing kept ones (non-conforming names still `null`, `formatMeetingDate`). No new criterion passed before implementation.

Implementer notes:
- T6 trims ASCII spaces only (`/^ +| +$/g`), not `String.trim()`, mirroring FR-1 so a tab next to a separator still fails and the file shows as non-conforming, matching Rust. No test pins this — T2's Rust author must read it the same way or the mirrors diverge silently.
- T7 implementer confirms `api.test.ts` has ZERO `updateVaultEntry` occurrences, so `kind: update.kind ?? null` ships untested. Fixer scope at the gate: assert the invoke payload for a present and an absent `kind` (expect `null`).
- T7 also trims ASCII spaces only, matching T6 and `vault::paths`. Untested on both sides.
- `parseMeetingName("260812 - - K")` (empty title) is untested in TS; the implementation rejects any empty section.
- T2 found `tests/acceptance.rs` is red in TWO places, not the one the blueprint predicted. Besides `fr02_fr03_pure_parser_splits_on_first_two_separators_with_no_filesystem`, the case `fr05_calendar_rejections_and_verbatim_date_in_the_folder_name` asserts `ELS - 2026-08-12 - x.mp4` → `DateNotSixDigits`, but a four-digit-year date carries two hyphens, so the name is now five sections → `TooManySeparators`, judged before the date validator. The expectation is obsolete, not the implementation. T4 rewrites both (keep a `DateNotSixDigits` case with a hyphen-free bad date such as `ELS - 26081 - x.mp4`).
- T8 found a pre-existing trap in `MeetingEditor.tsx`: the two `<select>` sentinels are literal NUL bytes (`"\0unsorted"`, `"\0new"`), so git treats the file as binary and any editor round-trip that renders them as spaces silently breaks them. Untouched by this feature; a later cleanup could use explicit `"\u0000..."` escapes.
- T8's `getByLabelText(/type/i)` is unambiguous only while no other label in the form contains "type".
- T3 made an OUT-OF-CONTRACT edit: `crates/vault/src/manage.rs:196` calls `meeting_folder_name`, and the signature change broke the crate's compilation, so it appended `None` with a comment marking it a transient T3→T4 bridge. T4 owns that file and rewrites the line with `update.kind`. Evaluator should confirm the bridge is gone.
- T4 confirms the T3 bridge in `manage.rs` is gone. Pre-existing wart left untouched: `layout::ensure_project_dir` runs before `check_move_length`, so an over-long valid rename can leave an empty project dir. The acceptance path-cap helper computes from `vault.root()` while `rename_meeting` measures the canonicalized root — equal here, would drift behind a junction or 8.3 path.
- SCOPE DECISION (orchestrator): FR-7 c2 requires the unsorted *reason* to reach the job message, but the app never surfaced any unsorted reason — `jobs.rs` maps the classification to the string "unsorted" and sets `message` only from collision/poll paths. The reason is available on `IngestOutcome.classification`. T5's Files list did not include `jobs.rs`; since FR-7 c2 is in the APPROVED blueprint, I widened T5 by that one file rather than dropping the criterion. Pre-existing gap, not type-specific — the new `TooManySeparators` route merely makes it visible.
- T5 test author notes: `update_vault_entry_handler` takes 5 params today and the tests call it with 6 (`kind` last), a second red that only surfaces once the lib compiles. FR-7 c4 (the CLAUDE.md sentence) has no test by design. The unsorted-placement case asserts a stem suffix, not the whole folder name, because `tests/common` cannot inject an ingest date (T4's acceptance file pins the full literal).
- T5 implementer: 5 of 8 cases red, cause is in the TEST harness, not the implementation. `drop_and_wait` assumes the meeting is idle after transcription, but `state_over` uses `FakeService::with_timing(instant_timing())` whose `/health` reports `llm_model_present: true`, so `queue_follow_up` registers a `summarize` job and the (correct, pre-existing) `has_active_job_for` guard refuses the rename. Fix is one line in the test harness: `FakeService::with_llm_model_absent()`. The implementer verified all five scenarios pass with only that swap, via a temporary probe binary (since deleted). Relaxing the rename guard is NOT an option.
- T5 chose to join both facts when an unsorted ingest also collides: `"{reason}; {collision}"`, reason first, using the Rejection's own Display verbatim.
