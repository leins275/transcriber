# Test wave log (Phase C.2) — batch 260909-trn-260909-spec

Format: feature / test file — cases; red reason; immediately-green; gaps / notes for the evaluator.

## F1 search-index-in-vault
- T1 tests/test_legacy_index_cleanup.py — 11 cases, 8 red / 3 green (negative invariants: stdout clean; silent when no orphan; startup survives unremovable orphan). Uses a collecting Handler on the `transcription` logger instead of caplog (propagate=False). Helper referenced as `index_db.remove_legacy_app_dir_index`; resolve() equality exercised via a `data/../data/index.sqlite3` spelling.

## F2 speaker-tagged-embeddings
- T1 tests/test_index_db_speakers.py — 17 cases, all red (ChunkRecord has no `speakers`, no `known_speakers`). Notes: `best_chunk_for` with unknown speaker asserted `is None` (no first-chunk fallback) — adjudicate if implementer disagrees; `schema_stale` asserted only on read-only handles.
- T2 tests/test_indexer_speakers.py — 9 cases, 7 red / 2 green (FR-2 "unchanged" invariants: notes breadcrumb unchanged; no-label transcript breadcrumb). Gap: FR-2 "make_snippet output unchanged" is owned by no task's test file. Override fixture overrides ids 0 AND 2 (needed for "no longer under the label").
- T3 tests/test_search_speakers.py — 14 cases, red (module missing). Note: `extract_query_speakers(text, known)` second param POSITIONAL (differs from extract_query_dates keyword-only); `known` may be a frozenset. «марка машины» deliberately not asserted.
- T5 tests/test_api_chat_speakers.py — 6 cases, 3 red / 3 green (negative half of FR-5: no known name → unscoped; unknown name; name absent from project). Fixture adds a third meeting (Марк Орлов) so the union case is falsifiable.
- T6 tests/test_mcp_server_speakers.py — 5 cases, 4 red / 1 green (no-filter regression guard). FastMCP silently ignores unknown args, so red is on result sets. Stale-index case needs schema bump + read-only `schema_stale` + `_Vault.index()` change.
- T4 tests/test_api_search_speakers.py — 7 cases, 4 red / 3 green (no-filter unchanged; blank speaker = no filter; wire shape unchanged). CORRECTION: blueprint says overlong speaker → 422, but the app answers 400 `invalid_request` for RequestValidationError — test asserts 400. Implementer must not return 422.

## F3 job-progress-accuracy
- T6 src/components/JobRow.test.tsx — 9 new cases (12 existing kept), 4 red / 7 green (FR-8 b4 stale-phase/no-bar on queued|done|failed; clamp 1.7→100%). Constraint enforced: no `%` text anywhere when progress is null. tsc red on `phase` until types.ts changes.
- T2 tests/test_diarizer.py — 16 new cases (17 existing kept), 15 red / 1 green (no-listener case, vacuous). CONFLICT: T2 task text says speaker_counting/discrete_diarization step-finished calls → None, but FR-4 b4 says artifact+no counts → 1.0 (verified against pyannote 3.4 call shapes); tests follow FR-4 (uniformly 1.0). Implementer must follow FR-4, not the T2 case list.
- T5 src-tauri/tests/job_phase.rs — 8 cases, red (27 compile errors: no `phase`, progress not Option). Pins `from_wire(status, progress, phase, error_kind, error_message)`; real type is `HttpTranscriptionService` (blueprint said HttpService); `JobSnapshot.progress` is ALREADY Option<f64> — don't add a second field; `#[serde(default)]` on `progress` untested but required per blueprint. Self-contained (no mod common).
- T3 tests/test_jobs_diarization.py — 7 new cases (1 old scale case replaced per blueprint), 6 red / 1 green (case 7 returning-voice guard, by design). fakes.py FakeDiarizer gained `on_progress`, `phases`, `blocked`+`release` Events (two events, not one — needed to avoid sleeps), BLOCK_TIMEOUT_SEC, and a cancel check after the wait. Red: 0.9 scale (0.675 != 0.75) and missing `JobState.phase`.
- T4 tests/test_llm_jobs.py — 7 new cases (21 existing kept), all red (no `JobState.phase`). Labels pinned: `reading transcript`, `writing summary · 3 tokens`, `summarizing part 2/3 · 3 tokens`, `writing export.md`, `rendering PDF`; finished → progress 1.0 / phase None. Note: FakeLlm already accepts `on_token`; `_complete_text` must pass it. "3 tokens" depends on FakeLlm slicing len//3. Gap: FR-2 monotone denominator on split-retry not expressible.
- T1 tests/test_jobs_phase.py — 10 cases (11 ids), 9 red / 2 green (clamp regression guards 1.7→1.0, -0.2→0.0). Red: JobStatus rejects progress=None; `phase` key missing from GET /v1/jobs; CLI prints no `phase:` line. Pins: queued → 0.0/null; terminal (succeeded/failed/ledger-rebuilt) → phase null; transcribe `preparing` keeps numeric 0.0; first provider fraction clears phase; CLI prints `phase: <label>` line and no progress line when progress is null.
- T7 src/components/AppHeader.test.tsx — 3 new cases (5 existing kept), 1 red / 2 green (vacuous negatives: percent wins; queued shows neither). CONFLICT: T7 says `activeJob.test.ts` stays untouched, but adding `phase: string | null` to ActiveJobView breaks two `toEqual` whole-view assertions there — a fixer (not the implementer) must add `phase: null` to those two expected objects, or the evaluator adjudicates.

## F4 selection-menu-viewport-clamp
- T1 src/lib/viewportPosition.test.ts — 9 cases, red (module missing; collection failure). 
- T2 src/components/SelectionSpeakerMenu.position.test.tsx — 6 cases, 5 red / 1 green (unmount case vacuously green until hook exists). Gap: FR-2 c4 "placed before first paint (useLayoutEffect)" not expressible in jsdom — review only. Stub measures every element as 200x40.

## F5 per-turn-speaker-reassign
- T1 src/lib/turns.test.ts — 5 new cases (28 existing kept), all red (function missing). Note for T3: adjacent Maxim segments merge into ONE turn regardless of pause; a "2 turns of Maxim" fixture needs another speaker in between. tsc red until export exists.
- T2 src/components/SpeakerTag.test.tsx — 20 cases (new file), 16 red / 4 green (FR-6 unchanged-behaviour guards). PINS: input aria-label `Edit speaker for this turn` (was `Rename <name>`); hints must match /choose the scope/i and /this turn only/i; chooser buttons `Only this turn` / `All N turns of <name>`; `turnsHeld={0}` passed for unattributed turns. tsc red until `turnsHeld` prop exists.
- T3 src/components/TranscriptViewer.test.tsx — 8 cases (2 rewritten, 6 new; 29 existing kept), all red on the aria-label gate. Fixtures rebuilt with separator turns (SPEAKER_2_TWICE={0,2}, MAXIM_TWICE={0,1,3}); `turnsHeld` MUST be counted via `speakerTurnCounts(groupIntoTurns(...))` (turns, not segments).

## F6 project-speaker-roster
- T2 src/lib/roster.test.ts — 15 cases, red (module missing). CONTRACT fixed by tests: add/remove/mergeRosterName operate on `ProjectRosterView` (mode preserved), not string[]; `pickerSource` in roster mode with empty roster returns `{roster: [], suggestions: []}`; open mode ignores roster names. FR-2 fixture shared verbatim with roster.rs (MESSY_NAMES/NORMALIZED_NAMES). Limits (200 chars / 500 names) are Rust-only by design.
- T7 src/components/ProjectRosterPanel.test.tsx — 14 cases, red (module missing). CONTRACT: props `{roster: ProjectRosterView, siblingNames: string[], onSave(roster)=>Promise<void>, onClose()}` — prop is `onClose` (blueprint T7 Implement said onCancel; majority says onClose), called after successful save AND on Cancel; panel never sees the project code. Accessible names: group `Who can be named in this project`, radios /anyone/i & /only the roster below/i, textbox `Add a name`, buttons `Add`, `Remove <name>`, `Add names already used in this project`, `Save`, `Cancel`; refusal shown in role=alert. Gap: 'reopening shows persisted roster' is T9's.
- T5 src/components/SelectionSpeakerMenu.roster.test.tsx — 6 cases, 4 red / 2 green (known buttons survive; open mode = today). PINS: roster select aria-label exactly `Attribute selection to a speaker` vs open-mode input `Attribute selection to a new speaker` (both report role combobox); placeholder option value="" ; empty-roster option disabled. Risk: SpeakerNameField swallowing Escape would break popover dismissal — untested.
- T3 src/components/SpeakerNameField.test.tsx — 14 cases, red (module missing). CONTRACT: `onCommit` receives RAW untrimmed text (blanks included — SpeakerTag maps blank → onAssign(null)); so T5 must keep SelectionSpeakerMenu's trim/blank guard in `commitDraft`, not wire onCommit=onAssign directly. Placeholder option value="" first; off-roster current value appended last; empty roster → single disabled option matching /empty/i; `commitOnBlur` prop; Escape → onCancel; `autoFocus`.
- T1 src-tauri/tests/roster.rs — 12 cases, red (unresolved `commands::roster`). CONTRACT: `ProjectRosterInput { mode: RosterMode, names: Vec<String> }`; `RosterMode` derives Debug+PartialEq; view fields public; file `<root>/<PROJECT>/roster.json` with schema_version 1; 64 KiB read cap pinned (65 KiB valid JSON → open roster); refuses all RESERVED_PROJECT_DIR_NAMES (incl. reports); save never creates a project; chats listing regression guard; `#[allow(dead_code)] mod common;` needed. Boundary (exactly 200 chars / 500 names) not covered.
- T6 src/components/TranscriptViewer.roster.test.tsx — 4 cases, 2 red / 2 green (FR-5 open-mode guards). Note: `input[list]` reports role combobox in aria-query 5.3 — `getByRole("textbox")` stops matching once suggestions exist; datalist options carry no `option` role while select options do (used as the discriminator).
- T8 src/state/useProjectRoster.test.ts — 7 cases, red (module missing). CONTRACT: `vi.mock("../api")` boundary with `api.readProjectRoster(project)` / `api.saveProjectRoster(...)`; hook `useProjectRoster(project)` exposes roster + `save(roster)` (ONE arg; App.tsx must adapt to RecordingPage's `onSaveRoster(project, draft)`); `save` REJECTS with the AppError on refusal and leaves roster untouched; save sends RAW draft, roster becomes the RETURNED normalized list (no local normalization); stale-response guard required; non-project recording → `{mode:"open",names:[]}` with no vault read.
- T9 src/components/RecordingPage.roster.test.tsx — 8 cases, 6 red / 2 green (project:null absence guards). PINS: breadcrumb toggle button named `Project speakers`; page-level select `getByRole("combobox", {name: "Name this speaker"})` — SpeakerNameField's <select> must carry the same aria-label as the input and must not set multiple/size; `onSaveRoster(project, draft)` on RecordingPage; panel seeded with names used in project. Placement beside pill → T11 manual.
- T4 src/components/SpeakerTag.roster.test.tsx — 11 cases, 8 red / 3 green (open-mode + known-button guards). PINS: `roster?: string[]` prop on SpeakerTag; select queried as combobox with the SAME aria-labels as the input (`Name this speaker` / `Rename <name>`); roster-mode hint matches /roster/i and no longer shows `Enter to name`; Escape must NOT commit via blur when the focused select unmounts (hazard — fix in component). Placeholder option filtered (blank value or disabled).
- T10 src/App.roster.test.tsx — 7 cases, 4 red / 3 green (FR-5/FR-7 degradation guards). PINS: commands `read_project_roster` {project} / `save_project_roster` {project, roster}; RecordingPage props `projectRoster` / `onSaveRoster(project, roster)`; roster read once per project; save result (not the draft) becomes the offered names. Teardown: `settle()` moved into afterEach before clearMocks — implementers must not restore per-test calls.

## Test wave summary
29 files written, all red at least in part. Immediately-green cases are negative/unchanged-behaviour guards throughout (listed per file). Requirement corrections made by test-authors: F2 T4 422→400; F3 T2 speaker_counting/discrete_diarization → 1.0 per FR-4; F3 T7 activeJob.test.ts needs `phase: null` in two toEqual (fixer scope); F5 fixtures need separator turns; F6 T7 prop is `onClose`; F6 T8 `save(roster)` one-arg + rejects on refusal.

## Environment notes
- Worktrees under specs/_intake/260909-trn-260909-spec/worktrees/<slug>; core.longpaths enabled; apps/desktop/node_modules is a junction to main; Python venvs synced per worktree (default extras only — no torch/pyannote/llama in worktree envs).
- Rust agents use CARGO_TARGET_DIR=D:/Local/Git/transcriber/target.
